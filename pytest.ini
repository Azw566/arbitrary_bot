[pytest]
markers =
    unit: pure unit tests — run offline, no network or RPC required
    integration: integration tests that require a live RPC endpoint (.env configured)

testpaths = tests
addopts = -v
